# Future SaaS HTTP API (`apps/api`)

**Status:** reserved placeholder — **not implemented**.

Public web scans run via **Netlify Functions** (`netlify/functions/scan`), not a standalone Node server.

This folder is reserved for a later multi-tenant SaaS API (auth, billing, queues). Do not add a `package.json` until that work starts.

See `netlify/functions/README.md` and [saas-expansion.md](../../docs/architecture/saas-expansion.md).
